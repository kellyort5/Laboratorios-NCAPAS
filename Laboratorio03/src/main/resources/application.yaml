spring:
  application:
    name: Laboratorio03
  datasource:
    url: jdbc:postgresql://localhost:5432/hyrule
    username: postgres
    password: root

  jpa:
    show-sql: false
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect